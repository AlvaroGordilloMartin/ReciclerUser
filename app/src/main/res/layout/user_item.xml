<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@android:color/holo_blue_light"
    android:layout_margin="@dimen/user_item_cardView_margin"
    app:cardCornerRadius="@dimen/user_item_cardView_cornerradius">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="@dimen/user_item_linearlayout_padding">

        <TextView
            android:textColor="@color/white"
            android:id="@+id/tvName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/user_item_tvName_paddingBottom"
            android:textSize="@dimen/user_item_tvName_textSize"
            android:textStyle="bold"
            tools:text="Nombre del usuario" />

        <TextView
            android:textColor="@color/white"
            android:id="@+id/tvEmail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Email del usuario" />
    </LinearLayout>
</androidx.cardview.widget.CardView>